services:
  - type: web
    name: guardgate-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main.py:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0